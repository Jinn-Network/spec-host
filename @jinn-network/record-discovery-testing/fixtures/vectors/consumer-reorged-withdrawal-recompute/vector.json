{
  "name": "consumer-reorged-withdrawal-recompute",
  "kind": "consumer",
  "description": "§5.1/§6.2: a `reorged` withdrawal is the one reason code that must trigger consumer recompute (the substrate moved), even though the item is still not silently pruned.",
  "input": {
    "itemKind": "https://spec.jinn.network/records/delivery/v1",
    "withdrawalReason": "reorged",
    "itemPreviouslyIngested": true
  },
  "expect": {
    "prunedFromDecisionStore": false,
    "triggersRecompute": true
  }
}
