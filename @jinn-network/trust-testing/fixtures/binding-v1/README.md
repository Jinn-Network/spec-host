# binding-v1

`example.json` is a sealed key-binding record (§7.1) generated by
`buildKeyBindingFixture` (`src/fixtures.ts`) and pinned as a static schema
golden. It validates with `validateKeyBinding` from `@jinn-network/trust-core`.
