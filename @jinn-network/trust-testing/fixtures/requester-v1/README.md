# requester-v1

The §7.5b requester authentication check (`authenticateRequester` from
`@jinn-network/trust-core`) positive and negative fixtures are executable
in `src/adversarial.test.ts`, including the leaked-document-replay and
audience-authentication-failure adversarial cases (§16).
