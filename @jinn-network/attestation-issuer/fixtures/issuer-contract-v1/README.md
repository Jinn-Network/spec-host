# Attestation Issuer contract fixtures v1

These deterministic envelopes exercise the package's exact serialization and binding contract.
Their fixed signature bytes are synthetic. They are not a cryptographic signature, identity
proof, authorization statement, or trust evidence.

`expected-digests.json` contains SHA-256 digests over the exact UTF-8 envelope files.
